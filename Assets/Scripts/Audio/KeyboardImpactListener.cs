using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*using Corale.Colore.Core;
using Color = Corale.Colore.Core.Color;

public class KeyboardImpactListener : AudioImpactListener {

    public override void AudioImpact( float reguestedCallbackValue ) {
        if (Settings.ADD_CHROMA_SUPPORT) {
            Keyboard.Instance.SetAll( new Color(
                Random.Range( 0, 0.1f ), 
                reguestedCallbackValue, 
                Random.Range( 0, 0.1f )) );
        }
    }
}
*/